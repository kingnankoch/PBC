C51 COMPILER V9.54   DS18B20                                                               01/26/2022 04:59:28 PAGE 1   


C51 COMPILER V9.54, COMPILATION OF MODULE DS18B20
OBJECT MODULE PLACED IN .\Objects\DS18B20.obj
COMPILER INVOKED BY: D:\Program Files\Keil_v5\C51\BIN\C51.EXE DS18B20.c OPTIMIZE(8,SPEED) BROWSE DEBUG OBJECTEXTEND PRIN
                    -T(.\Listings\DS18B20.lst) TABS(2) OBJECT(.\Objects\DS18B20.obj)

line level    source

   1          #include "reg51.h"
   2          // #include "stdio.h"
   3          
   4          
   5          
   6          // ------ lcd  start-----
   7          sbit RS=P3^0;
   8          sbit RW=P3^1;
   9          sbit E=P3^2;
  10          // sbit DQ=P3^3;
  11          unsigned int readtemp = 0;
  12          unsigned char str[]={"0123456789"};
  13          unsigned char s[]={"Temperature:"};
  14          
  15          
  16          // ------ lcd  end-----
  17          
  18          // 温度传感器的
  19          sbit DQ = P3^3;
  20          
  21          
  22          
  23          // 延时函数 因为 DS18B20函数的 时序 中的 在读的0的模式需要 15ms 在读1 的情况下�
             -��要 5ms左右
  24          void delay_18B20(unsigned int i){
  25   1          for (; i > 0; i--);
  26   1      }
  27          
  28          
  29          
  30          
  31          // 初始化 DS18B20
  32          
  33          // 精度分为 9、10、11、12 位，默认 12位
  34          // 在配置过程中 一般跳过ROM 指令码 OCCH
  35          // 启动温度转换 指令 44H
  36          // 读暂存器 0BEH 
  37          // 首先将 数据线下拉持续 时间 480us-960us 后然后释放（拉高电平）DS18B20收到信号
             -后等待15-60us左右 初始化成功，会在 12-60us后产生一个低电平，信号会持续 60-240us
  38          void init_DS18B20(void){
  39   1          unsigned char x = 0;
  40   1          DQ = 1; 
  41   1          delay_18B20(8);//拉低电平前 先保持高电平一段时间
  42   1          DQ = 0;
  43   1          delay_18B20(80);//拉低电平保持 480us-960us 后然后释放（
  44   1          DQ = 1; //拉高，等待 DS18B20反应
  45   1          delay_18B20(14); // 等待12-60us 后查看数据线的状态
  46   1          x = DQ;//如果 是初始化成功，那么会产生一个低电平 若X= 0表示成功
  47   1          delay_18B20(20); //60-240us
  48   1      
  49   1      }
  50          
  51          
  52          // 读时序
C51 COMPILER V9.54   DS18B20                                                               01/26/2022 04:59:28 PAGE 2   

  53          // 读时序分为读1和读0，读1两个过程，当要读区DS18B20的数据时，先将总线拉低，
             -并保持 1us 然后总线拉高，此时应尽快读取，从拉低到读区引脚状态时间不能超过15us
  54          //读1的情况下，只能将电平拉低1us，读0的情况下可以将电平拉低到15us左右，但�
             -�能超过 15us
  55          unsigned char ReadOneChar(void){
  56   1          unsigned char i = 0;
  57   1          unsigned char dat = 0;
  58   1          for(i = 8;i > 0; i--){
  59   2              DQ = 0; //先拉低总线
  60   2              delay_18B20(8);
  61   2              dat >>= 1; //每次读取1位后，数据向右移位 ；移位处理的时间需要1us以上，�
             -�以不需要再延时
  62   2              DQ = 1; //拉高总线
  63   2              delay_18B20(8);
  64   2              if(DQ == 1){ //如果DQ是1
  65   3                  dat |= 0x80; //把数据第一位变成高位
  66   3                  delay_18B20(4); //等待60us
  67   3              }
  68   2          }
  69   1          return (dat);
  70   1      }
  71          
  72          // 写时序
  73          // 写时序包含写0和1，
  74          // 写0的过程中要把总线的时间拉低60us 保证  DS18B20能在 15us-45us之间正确采样I/O总
             -线上的 0电平
  75          // 在写 1的时候要在15us以内拉高电平，
  76          void WriteOneChar(unsigned char dat){
  77   1          unsigned char i=0;
  78   1          for (i = 8; i > 0; i++) {
  79   2              DQ = 0;
  80   2              DQ = dat & 0x01;
  81   2              if(DQ) {
  82   3                  delay_18B20(1);
  83   3                  DQ = 1;
  84   3              } else {
  85   3                  delay_18B20(5);
  86   3                  DQ = 1;
  87   3              }
  88   2              // 例如5>>=1,首先将5转换为2进制，结果是0101，右移一位就成了0010，转换回�
             -��进制就成了2了，就这样
  89   2              dat >>= 1;
  90   2          }
  91   1      }
  92          
  93          // 读取 温度信息
  94          unsigned char ReadTemperature(){
  95   1          unsigned char a = 0,b = 0;
  96   1          unsigned int temp = 0;
  97   1          // 初始化
  98   1          init_DS18B20();
  99   1          // 写入数据 
 100   1          WriteOneChar(0xCC); // 跳过读序列号操作
 101   1          WriteOneChar(0x44); // 启动温度转换
 102   1          delay_18B20(100); //等待一段时间后再进行初始化 ？？
 103   1          init_DS18B20();  // 初始化
 104   1          WriteOneChar(0xCC); //再一次跳过读序列号
 105   1          WriteOneChar(0xBE); // 读取温度寄存器
 106   1          delay_18B20(100); // 再次 等待一段时间
 107   1          a = ReadOneChar(); //读取温度的 低位
 108   1          b = ReadOneChar(); //读取温度的 高位
 109   1          //因为读取到的数据是二进制的 
C51 COMPILER V9.54   DS18B20                                                               01/26/2022 04:59:28 PAGE 3   

 110   1          // 采集的温度除16得到实际温度, 因为只采集到12位数据，高四位是没有数据的
 111   1          temp = ((b * 256 + a) >> 4); 
 112   1          return(temp -10);
 113   1      }
 114          
 115          // ------ lcd ------------
 116          
 117          void delay(unsigned int n)
 118          {
 119   1        unsigned int i=0,j=0;
 120   1        for(i=0;i<n;i++)
 121   1        {
 122   2          for(j=0;j<120;j++);
 123   2        }
 124   1      }
 125          
 126          
 127          void writedat(unsigned char dat)
 128          {
 129   1        RS=1;
 130   1        RW=0;
 131   1        E=0;
 132   1        P1=dat;
 133   1        delay(5);
 134   1        E=1;
 135   1        E=0;
 136   1      }
 137          
 138          void writecom(unsigned char com)
 139          {
 140   1        RS=0;
 141   1        RW=0;
 142   1        E=0;
 143   1        P1=com;
 144   1        delay(5);
 145   1        E=1;
 146   1        E=0;
 147   1      }
 148          
 149          void initlcd()
 150          {
 151   1        writecom(0x38);
 152   1        writecom(0x0c);
 153   1        writecom(0x06);
 154   1        writecom(0x01);
 155   1      }
 156          
 157          void display()
 158          {
 159   1        unsigned char temp0=0,temp1=0,temp2=0,i=0;
 160   1        temp0=readtemp/100;
 161   1        temp1=(readtemp%100)/10;
 162   1        temp2=readtemp%10;
 163   1        
 164   1        writecom(0x80);
 165   1        delay(5);
 166   1        while(s[i]!='\0')
 167   1        {
 168   2          writedat(s[i]);
 169   2          delay(5);
 170   2          i++;
 171   2        }
C51 COMPILER V9.54   DS18B20                                                               01/26/2022 04:59:28 PAGE 4   

 172   1        writecom(0x80+0x40+6);
 173   1        writedat(str[temp0]);
 174   1        delay(5);
 175   1        writedat(str[temp1]);
 176   1        delay(5);
 177   1        writedat(str[temp2]);
 178   1        delay(5);
 179   1        writedat('C');
 180   1        delay(5);
 181   1      }
 182          
 183          
 184          // ------ lcd  end-----
 185          
 186          
 187          
 188          int main()
 189          {
 190   1          // printf("hellow world");
 191   1          initlcd();
 192   1          while (1)
 193   1          {
 194   2              /* code */
 195   2              // readt
 196   2            readtemp=ReadTemperature();
 197   2            display();
 198   2      
 199   2      
 200   2          }
 201   1          
 202   1          /* code */
 203   1          // return 0;
 204   1      }
 205          


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    434    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =   ----    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =     26       3
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
